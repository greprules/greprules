name: greprules
version: "0.4.0"
description: "Fetch greprules.io rule packs, run OpenGrep scans, and inject compact SAST findings into Hermes turns."
author: "provally"
hooks:
  - post_tool_call
  - pre_llm_call
